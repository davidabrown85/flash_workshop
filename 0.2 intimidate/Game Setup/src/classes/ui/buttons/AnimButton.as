package classes.ui.buttons{		import flash.display.MovieClip;	import flash.events.MouseEvent;	import flash.events.Event;	import flash.text.TextField;		public class AnimButton extends BasicButton	{				public function AnimButton():void 		{		}				protected function mouseDown( ev:MouseEvent ):void		{			ev.stopPropagation();						if( currentFrame == 1)				gotoAndPlay(2);		}	}	}