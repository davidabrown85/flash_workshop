package classes.ui.loader{	import flash.display.Sprite;	import flash.events.Event;		public class LoadingSpin extends Sprite	{		public function LoadingSpin()		{			//init();					}		public function init(  ):void		{			//addEventListener( Event.ENTER_FRAME, update_spin );			//addEventListener( Event.REMOVED_FROM_STAGE, clean_up );		}				private function update_spin( ev:Event ):void		{			rotation += 5;		}					public function clean_up( ev:Event = null ):void		{			///removeEventListener( Event.ENTER_FRAME, update_spin );			//removeEventListener( Event.REMOVED_FROM_STAGE, clean_up );		}	}}